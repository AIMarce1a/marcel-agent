#
# docker-compose.windows.yml — Windows Docker Desktop compatible
#
# Differences from docker-compose.yml:
#   - Removes `network_mode: host` (not supported on Docker Desktop for Windows)
#   - Uses explicit port mappings instead
#   - Uses Windows-style volume path for ~/.marcel
#
# Usage:
#   docker compose -f docker-compose.windows.yml up -d
#
services:
  gateway:
    image: AIMarce1a/marcel-agent:latest
    container_name: marcel
    restart: unless-stopped
    volumes:
      - ${USERPROFILE}/.marcel:/opt/data
    environment:
      - MARCEL_UID=10000
      - MARCEL_GID=10000
    command: ["gateway", "run"]

  dashboard:
    image: AIMarce1a/marcel-agent:latest
    container_name: marcel-dashboard
    restart: unless-stopped
    depends_on:
      - gateway
    volumes:
      - ${USERPROFILE}/.marcel:/opt/data
    environment:
      - MARCEL_UID=10000
      - MARCEL_GID=10000
      - MARCEL_DASHBOARD_HOST=0.0.0.0
    ports:
      - "127.0.0.1:9119:9119"
    command: ["dashboard", "--host", "0.0.0.0", "--port", "9119", "--no-open", "--insecure"]
